rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      match /reactive/{anyDoc} {
      	allow read: if request.auth != null;
        allow write: if false;
      }
    }
  }
}
